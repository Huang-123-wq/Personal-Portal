- **安装命令**：使用 `npm install -g ui-ux-pro-max-cli` 安装全局 CLI，并通过 `uipro init --ai <platform>` 为特定 AI 助手配置技能。
- **设计系统生成**：执行 `python3 scripts/search.py "<query>" --design-system -p "Project Name"` 可自动生成包含模式、风格、配色和字体的完整设计方案。
- **持久化存储**：支持 `--persist` 参数将设计系统保存为 `MASTER.md` 和页面级覆盖文件，实现跨会话的分层检索。