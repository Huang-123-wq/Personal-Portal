- 采用“主文件 + 覆盖文件”（Master + Overrides）的模式管理设计规范，页面级规则优先于全局 MASTER.md。
- 所有视觉元素推荐使用 SVG 矢量图标，严禁在结构性 UI 中使用 Emoji 以确保跨平台一致性。
- 交互反馈遵循严格的时序规范，微交互时长控制在 150-300ms，并优先使用 transform/opacity 以保证性能。
- 色彩与排版必须使用语义化 Token（如 primary, surface），禁止在组件中硬编码原始十六进制颜色值。